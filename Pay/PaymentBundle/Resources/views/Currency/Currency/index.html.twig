{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Currency\Currency list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Abbreviation</th>
                <th>Currencyratio</th>
                <th>Dafault</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('currency_currency_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.abbreviation }}</td>
                <td>{{ entity.currencyRatio }}</td>
                <td>{{ entity.dafault }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('currency_currency_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('currency_currency_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('currency_currency_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
