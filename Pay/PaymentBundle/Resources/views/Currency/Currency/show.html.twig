{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Currency\Currency</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Abbreviation</th>
                <td>{{ entity.abbreviation }}</td>
            </tr>
            <tr>
                <th>Currencyratio</th>
                <td>{{ entity.currencyRatio }}</td>
            </tr>
            <tr>
                <th>Dafault</th>
                <td>{{ entity.dafault }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('currency_currency') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('currency_currency_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('currency_currency_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
