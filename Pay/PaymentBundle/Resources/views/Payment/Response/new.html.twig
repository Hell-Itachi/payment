{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Payment\Response creation</h1>

    <form action="{{ path('itc_response_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('itc_response') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
